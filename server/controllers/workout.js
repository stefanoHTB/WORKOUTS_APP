const mongoose = require('mongoose');
const Workout = require('../models/Workout');
const express = require("express");




const createWorkout = async (req, res) => {
    try {
       console.log('hello')
    } catch(e) {

    }
}

module.export = { createWorkout }